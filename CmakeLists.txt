cmake_minimum_required (VERSION 2.6)
project (AVX-Gradients)
if(MSVC)
  # Multithreaded compilation
  set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /arch:AVX2")
endif()

add_executable(AVX-Gradients src/avx_gradients.cc)

